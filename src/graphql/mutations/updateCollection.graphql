mutation UpdateCollection(
  $id: String!
  $name: String
  $description: String
  $isPublic: Boolean
) {
  updateCollection(
    id: $id
    name: $name
    description: $description
    isPublic: $isPublic
  ) {
    id
    name
    description
    isPublic
    updatedAt
  }
}
