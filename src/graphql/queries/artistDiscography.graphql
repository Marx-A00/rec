query GetArtistDiscography($id: String!) {
  artistDiscography(id: $id) {
    id
    source
    title
    releaseDate
    primaryType
    secondaryTypes
    imageUrl
    artistName
    artistCredits {
      artist { id name }
      role
      position
    }
    trackCount
    year
  }
}