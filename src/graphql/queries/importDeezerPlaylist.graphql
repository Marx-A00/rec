mutation ImportDeezerPlaylist($playlistId: String!, $selectedDeezerIds: [String!]) {
  importDeezerPlaylist(playlistId: $playlistId, selectedDeezerIds: $selectedDeezerIds) {
    success
    message
    jobId
    playlistName
  }
}
