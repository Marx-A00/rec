mutation AddAlbumToQueue($albumId: UUID!) {
  addAlbumToQueue(albumId: $albumId) {
    success
    message
    error
    album {
      id
      title
      gameStatus
    }
    curatedChallenge {
      id
      sequence
    }
  }
}
