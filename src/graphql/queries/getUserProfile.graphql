query GetUserProfile($userId: String!) {
  user(id: $userId) {
    id
    username
    email
    image
    bio
    followersCount
    followingCount
    recommendationsCount
    isFollowing
    settings {
      profileVisibility
    }
  }
}
