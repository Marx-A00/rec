query GetUserProfile($userId: String!) {
  user(id: $userId) {
    id
    username
    email
    image
    bio
    role
    followersCount
    followingCount
    recommendationsCount
    isFollowing
    settings {
      profileVisibility
    }
  }
}
