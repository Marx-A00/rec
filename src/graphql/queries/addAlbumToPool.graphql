mutation AddAlbumToPool($albumId: UUID!) {
  addAlbumToPool(albumId: $albumId) {
    success
    message
    error
    album {
      id
      title
      gameStatus
    }
    curatedChallenge {
      id
    }
  }
}
